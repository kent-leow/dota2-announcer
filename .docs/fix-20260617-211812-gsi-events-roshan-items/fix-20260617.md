# Fix Log

> Generated: 2026-06-17 21:18:12
> Source task: .docs/dynamic-gsi-events/, .docs/dynamic-hero-items/

## Issues

- [x] **FIX-001** — Roshan tracker doesn't work: no persistent nor notification overlay shown <!-- fixed: 2026-06-17 -->
  _Source_: raw input
  _Files_: `application/src/dota/roshanTracker.ts`, `application/src/dota/gsiServer.ts`, `application/src/dota/gsiTypes.ts`
  _Root Cause_: The tracker relied on `data.map.roshan_state` field which does NOT exist in player-mode GSI. In actual gameplay, roshan data comes via the `data.events[]` array (`event_type: "roshan_killed"`) and the top-level `data.roshan` object (which is empty `{}`). The `roshan_state` field is spectator-only data.
  _Fix_: Parse `data.events[]` from GSI payload. Detect `roshan_killed` event_type to trigger kill notification. Implement timer-based respawn tracking (8-min base, 11-min max) for `may_respawn` and `respawn` events. Kept fallback support for spectator-mode `roshan_state` field.

- [x] **FIX-002** — Hero Items only shows player's items, no enemy items; no sound provided <!-- fixed: 2026-06-17 -->
  _Source_: raw input
  _Files_: `application/src/dota/itemsTracker.ts`, `application/src/main/ipcHandlers.ts`, `application/src/main/preload.ts`, `application/src/ui/main/MainDock.tsx`, `application/src/renderer/electron.d.ts`
  _Root Cause_: Player-mode GSI only provides the player's own inventory in `data.items` — enemy items are NOT available. Additionally, no IPC bridge existed to send item events to the renderer, and no TTS call was wired up.
  _Fix_: (1) Added `events[]` parsing to detect `aegis_picked_up` from GSI events array — this is the key enemy-relevant item event available in player mode. (2) Added IPC bridge (`dota:itemEvent` channel) from main process to renderer. (3) Added `onItemEvent` to preload API. (4) Added TTS announcements for item events in MainDock.

- [x] **FIX-003** — Bounty and day notification overlay shown twice when game starts (after pre-game) <!-- fixed: 2026-06-17 -->
  _Source_: raw input
  _Files_: `application/src/ui/main/MainDock.tsx`
  _Root Cause_: The `onStateChange('in-match')` handler called async `loadSchedule()` which resets `firedIds`. Due to the async gap, `tick()` fires events before `loadSchedule` completes, then `loadSchedule` resets `firedIds`, causing the next tick to re-fire the same events.
  _Fix_: Replaced async `loadSchedule()` with synchronous `eventScheduler.resetScheduler()` in the state change handler. The schedule is already loaded on mount via `getEvents()` — the transition only needs to clear fired tracking.

## Key Insight

Player-mode GSI (when you're playing, not spectating) has significant limitations:
- `map.roshan_state` / `map.roshan_state_end_seconds` — NOT available (spectator-only)
- `items` — only YOUR hero's inventory
- `events[]` array — the primary source of truth for game events like roshan kills, aegis pickups

The `events[]` array is the correct mechanism for detecting these events in player mode.

## Changelog

- 2026-06-17: FIX-001 — Roshan tracker now uses events array for kill detection + timer-based respawn tracking
- 2026-06-17: FIX-002 — Item tracker detects Aegis from events array, full IPC/TTS pipeline wired
- 2026-06-17: FIX-003 — Fixed duplicate notification overlay on game start by using sync resetScheduler instead of async loadSchedule
