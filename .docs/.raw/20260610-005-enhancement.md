1. introduce hero picking phase, pre-game phase and existing game start phase. introduce the more status (Idle, In Match, Hero Pick Phase, Pre Game).
2. Volume doesn't works, make it works, then introduce voice speed (0.5 to 3.0), same scroller.
3. Introduce more voice (currently is male), add one more girl voice (if possible).
4. Don't have icon in system tray, also update the app icon. Generate a dota2 announcer icon that suits the theme.
5. The GSI Setup is now in the User Guide, move it to main page, and introduce uninstall GSI (remove). Show connected/disconnected to GSI with corresponding button.
6. Make all the timing configurable in the app (do not hardcode with config, but a app cache/local data with default preset.) Configurable with a new tab, click reload config will reflect latest configuration.